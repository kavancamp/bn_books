class BnBooks::CLI 

    def call 
        puts "        ***** "
        puts "  Welcome To FreeBooks!!! "
        puts "        ***** "

        #categories
        #menu
    end

    def menu
        while input != "exit"
        Please "/nEnter a number, 'list' to display categories, or 'exit'"

        input = gets.strip
        if (input.to_i > 0) && (input.to_i < 10)
        
    end

end